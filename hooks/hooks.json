{
    "SessionStart": [
        {
            "matcher": "",
            "hooks": [
                {
                    "type": "command",
                    "command": "python -m devkit.cli memory snapshot --format hook --project \"$(basename $(git rev-parse --show-toplevel 2>/dev/null || pwd))\" 2>/dev/null || true"
                }
            ]
        }
    ]
}
